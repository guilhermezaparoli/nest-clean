import { HashGenerator } from '@/domain/forum/application/cryptography/hash-generator'

// export class AuthHashGenerator implements HashGenerator {
// async hash(plain: string): Promise<string> {}
// }
